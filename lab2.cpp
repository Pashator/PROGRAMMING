//вариант 2

#include <iostream>;

int main() {
    long long h, a;
    std::cin >> h >> a;
    double S = (static_cast<double>(a) * h) / 2.0;
    std::cout << S << std::endl;
    return 0;
}
